apiVersion: v1
kind: Service
metadata:
  name: patient-service
  namespace: staging
  labels:
    app: patient-service
    env: staging
spec:
  type: ClusterIP
  selector:
    app: patient-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
